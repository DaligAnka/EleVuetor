createApp(options).mount("div")
в options ...({data(){   Дата == state в реакте
    return...
}})

setup - для постоянного вызова скрипта. как в реакте.

Добавить лишний лифт или этаж

Vue.component('todo-item', {
  template: '\
    <li>\
      {{ title }}\
      <button v-on:click="$emit(\'remove\')">Удалить</button>\
    </li>\
  ',
  props: ['title']
})

new Vue({
  el: '#todo-list-example',
  data: {
    newTodoText: '',
    todos: [
      {
        id: 1,
        title: 'Помыть посуду'
      },
      {
        id: 2,
        title: 'Вынести мусор'
      },
      {
        id: 3,
        title: 'Подстричь газон'
      }
    ],
    nextTodoId: 4
  },
  methods: {
    addNewTodo: function () {
      this.todos.push({
        id: this.nextTodoId++,
        title: this.newTodoText






        <script setup>
const props = defineProps(['foo'])

console.log(props.foo)
</script>
  ||
  in non 'sccript setup' :
export default {
  props: ['foo'],
  setup(props) {
    // setup() receives props as the first argument.
    console.log(props.foo)
  }
}



v-for == for loop


watch || watcheffect чекают измненния в subscribers. в аргументах.

Благодаря Reactive будут отслеживаться зависимости и ref. И обновляться автоматически при изменении reactive состояния.
В реакте при рендере происходит экзекуция всех функций файла, а в вю только то, что меняется. Computed тоже работает в рамках этой, отличной от реактовской, философии.
(основы реактивности)